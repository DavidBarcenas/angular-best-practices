import { FlatObjectPipe } from './flat-object.pipe';

describe('FlatObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new FlatObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
