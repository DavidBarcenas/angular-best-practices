import { ToggleBanWordsPipe } from './toggle-ban-words.pipe';

describe('ToggleBanWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new ToggleBanWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
